<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="backend.modules.user.mapper.SysUserMapper">

    <resultMap id="UserRM" type="backend.modules.user.entity.SysUser">
        <id     property="id"         column="id"/>
        <result property="username"   column="username"/>
        <result property="password"   column="password"/>
        <result property="realName"   column="real_name"/>
        <result property="studentId"  column="student_id"/>
        <result property="phone"      column="phone"/>
        <result property="email"      column="email"/>
        <result property="avatarUrl"  column="avatar_url"/>
        <result property="role"       column="role"/>
        <result property="totalHours" column="total_hours"/>
        <result property="status"     column="status"/>
        <result property="createdAt"  column="created_at"/>
        <result property="updatedAt"  column="updated_at"/>
        <result property="deleted"    column="deleted"/>
    </resultMap>

    <select id="selectByUsername" resultMap="UserRM">
        SELECT * FROM sys_user
        WHERE username = #{username} AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectById" resultMap="UserRM">
        SELECT * FROM sys_user
        WHERE id = #{id} AND deleted = 0
        LIMIT 1
    </select>

    <select id="countByUsername" resultType="int">
        SELECT COUNT(*) FROM sys_user
        WHERE username = #{username} AND deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, real_name, student_id, phone, email, role)
        VALUES (#{username}, #{password}, #{realName}, #{studentId}, #{phone}, #{email}, #{role})
    </insert>

</mapper>
